문제1
const의 의미?
상수로 만드는것
const int num = 10; num은 10으로 상수화
const int * ptr1 = &val1; 포인터 ptr1을 이용해 val1의 값을 변경할 수 없음
int * const ptr2 = &val2; 포인터 ptr2가 상수화
const int * const ptr3 = &val3; 포인터 ptr3가 상수화 되며 포인터로 val3의 값을 변경할 수 없음.

문제2
실행중인 프로그램의 메모리 공간
데이터 - 전역변수 저장공간
스택 - 지역변수 및 매개변수가 저장되는 영역
힙 - malloc 함수호출에 의해 프로그램이 실행되는 과정에서 동적으로 할당이 이뤄지는 영역
// malloc > 동적으로 메모리할당을 하는 함수 자료형*malloc(사이즈)  이런식으로 씀. 그럼 주소의 시작값을 반환해줌
free - malloc을 통해 할당한 메모리 공간을 해제하는 함수

문제3
Call-by-value vs Call-by-reference
값으로 호출 , 참조로 호출이다.
값으로 호출하는 경우는 주소는 변하지 않고 그 주소에 할당되는 '값'만 바뀌게 된다.
참조로 호출하는 경우 해당 변수가 갖는 주소값이 변하는 방식이다.